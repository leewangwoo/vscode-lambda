global:
  scrape_interval: 5s # 5초마다 실시간 데이터 수집

scrape_configs:
  - job_name: 'litellm'
    static_configs:
      - targets: ['litellm-gateway:4000'] # LiteLLM 메트릭 포트

  - job_name: 'nvidia-gpu'
    static_configs:
      - targets: ['nvidia-gpu-exporter:9445'] # GPU 메트릭 포트